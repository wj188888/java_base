
> 抽象类
	如果自下而上的类的继承层次结构中上移，位于上层的类更具有一般性，可能更加抽象。

抽象方法：充当着占位方法的角色，它们在子类中具体实现。

抽象类没有构造器，所以不能构造一个抽象类的实例对象

---

> Object：所有类的超类

	Object类是Java中所有类的始祖，在Java中每个类都拓展了Object。但是并不需要显示表达。
如果没有明确地指出超类，Object就被认为是这个类的超类。由于在java中每个类都是由Object类拓展而来的。

Object类型的变量：
	对象变量都可以定义为Object。
	equals方法：检查一个对象是否等于另外一个对象。
